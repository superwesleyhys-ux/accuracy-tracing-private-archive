# Luna envelope v2 — invalidated

16 arms completed with one successful local GPT-5.6 Luna call per arm. Independent audit rejected the batch because the harness audit stored the temporal prefix inside `audit.instructions`; the verifier requires that field to contain only the body instruction. The field mapping is corrected for the next fresh policy. No accuracy comparison is reported from this batch.

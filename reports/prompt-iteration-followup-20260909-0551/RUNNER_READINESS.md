# Source-blind runner readiness — Candidate 2

Preparation only. This review does not admit another case, authorize inference, or establish that a mixed holdout is ready. A future run still needs an independently admitted false case and an independently reviewed affirmative true control, with exact claim scope, cutoff evidence and later adjudication frozen before inference. Do not reuse the exposed development pair as a substitute.

The existing Candidate 2 machinery can be reused in a **new experiment directory**. Preserve Round 2 and all earlier code, registrations, inputs and results. This checklist inspected the frozen runner/scorer and only a structural projection of the reviewed control input: field names, types and list cardinalities. No control claim, source identity, content, dates, quantities or gold labels are included here; control gold was not opened.

The control input uses the existing corpus schema: `schema_version`, a `cases` array, and case fields `target`, `claim_made_at`, `materials`, `initial_version_ids`, `config` and `variant`. Target fields are `id`, `text`, `as_of` and `source_version_id`. Material fields include version identity, URL, issuer, content, retrieval/publication/availability metadata. Publication is nullable. The structural compatibility issue is that a legitimate case can have one supplied version; the historical validator already permits this, while the Round 2 wrapper requires exactly two. Do not duplicate or manufacture a second snapshot to satisfy that requirement.

| Required change in the new experiment | Current location | Acceptance condition |
|---|---|---|
| Replace diagnostic-specific case IDs and phase with a strictly validated new plan. | `round-2/run_test.py` lines 38–43, 127–154; `round-2/summarize_test.py` lines 380–396 | Exactly two distinct opaque IDs, both arms, fixed order and claim research mode. Reject extra fields, duplicate/missing IDs and any role, truth label or answer hint in the worker policy. The registered plan determines membership; changing a case requires new registration. |
| Accept the actual number of admitted versions. | Driver lines 127–134 | Replace `len(materials) == 2` with a positive bounded count compatible with the existing document limits. Retain unique IDs, exact original anchor and all existing temporal checks. One-version and multiple-version fixtures must pass without padding; missing, duplicated, unknown or after-cutoff versions must fail. |
| Bind the new corpus and admission artifacts, not the old eight-case constants. | Driver lines 368–413, 675–706; scorer lines 20–24, 380–433, 451–461, 626–638 | New corpus, gold, input review, gold review and source-free admission/preflight commitments must be registered. Keep the unchanged scoring helper explicitly hashed. Replace hardcoded old review paths and default corpus/gold paths. The supervisor reads the corpus plus hashes/boolean admission metadata only; future gold remains scorer-only. Never replace hash checking with a bare “ready” flag. |
| Freeze the actual new driver location and complete inference inventory. | Driver lines 158–161, 467–490; scorer lines 424–433 | Update the copied worker path, runner identity and new experiment paths. Continue hashing/copying only the driver, required historical helper/transport and production inference modules. Archive and verify all registered bytes; no plan, protocol, scorer, registration, review prose, gold or source-research directories enter the worker. |
| Derive corpus and result denominators from the exact admitted plan. | Scorer lines 451–478, 626–638 | Require exactly two canonical cases and four ordered paired outcomes, or explicitly register a larger supervisor corpus with the exact two-case worker projection. The narrow two-case corpus is simpler. Remove the eight-case and `h01`…`h08` requirements. Reject missing/duplicate/reordered outcomes; never shrink the denominator after a failure. |
| Replace attribution-only role assumptions with private truth strata. | Scorer lines 572–616, 632–638 | Keep cutoff verdict and later factual outcome distinct. Only post-audit private gold may identify the affirmative true control and later-false case. Do not rename a genuine truth control as `attribution_control`. Preserve assertion scope separately so an attributed report is never silently scored as real-world authenticity. |
| Correct later-false metrics for mixed truth classes. | Scorer lines 585–586 | Current flags test `role == authenticity` but do not require a later-false gold label. In the mixed fork, require the independently adjudicated later-false stratum for “called false” and “accepted false”; separately count false accusations, acceptance, abstention, conflict and failure on the affirmative true control. A false accusation on a true authenticity claim must never earn detection credit. |
| Derive public scope and source counts from the frozen admission. | Driver lines 451–456; scorer lines 592–616, 650–680 | Replace “both versions,” one-family/eight-case metadata, exposed-wastewater flags and the old 2/2-ceiling sentence. Report actual family counts and exposure history. Two cases do not establish general superiority. Do not claim a heldout evaluation happened until the admitted batch is actually complete. |

Use a new explicit plan/schema version for the changed admission contract. Register an admission-metadata hash alongside corpus, gold, scorer, helper, independent reviews, driver, plan, protocol and code inventory. The admission metadata may contain opaque IDs and source-free pass/fail checks; keep per-case truth roles and supporting adjudication text in private gold/reviews. Bind both cases to independent review before launch. If annexures become available, their text can enter inference only if their exact version independently meets the cutoff; later investigative findings belong solely in gold. File creation or retrieval time alone is neither a substitute for historical availability nor a reason to reject an evidenced historical copy.

The following behavior should remain unchanged:

- Local Codex-login HTTP to hosted `gpt-6-astra`, medium reasoning, disabled local skill catalog and no API fallback. “Local” describes the route and orchestration, not offline model weights. Retain actual model/effort/route and skill-isolation receipts for both arms.
- The exact 3,904-character Candidate 2 guidance, SHA-256 `4c6936fe1e18426a00125a800fa98808bc583ccf7ef9b50d8af54773b866ef79`, once after the base harness `decompose`/`verify` instruction only. Keep the historical temporal prefix, direct baseline, other research instructions and output schemas unchanged. Independently retain the literal in the new scorer and audit every actual request.
- One explicit canonical claim per item, exact target text/cutoff/source anchor; `research_mode=claim`, depth 1, at most two research queries, zero live searches and zero live origin-following depth. Every research request receives all admitted eligible source versions in full, including exact offsets, lengths and availability metadata. Keep the 240,000-character fail-before-call capacity; no silent prefixes or windows. Direct receives the entire same canonical pool; formal verification remains adaptive.
- Bounded untrusted research advice, at most 6,000 serialized characters, with candidate IDs restricted to canonical versions. Advice cannot admit evidence, establish origin, resolve a gap or supply a quotation. Same-URL versions remain distinct versions, not independent corroboration.
- At most one direct call and 24 shared harness calls per case, including at most 10 formal calls; 90 seconds per call, existing 120/2,190-second case deadlines, 50 maximum logical batch calls and sequential rotating arm order. Natural early completion is valid; do not require a second loop when no second loop executed. Record actual phase skips and loops.
- Exact source/target matching, native research-schema validation, actual request/response/receipt binding, exact unique eligible quotations and offsets, and strict whole-pipeline failure accounting. Formal-only validity remains a separate diagnostic. Research/collection/schema failures cannot receive primary correctness, abstention or detection credit.
- Per-case/stage and total accounting for every attempted target-model invocation. Unknown usage stays null with known lower bounds; cached input is already included in input tokens. Preserve failed attempts, disallow output overwrite or selective replacement, and disclose that internal CLI retries and this Codex task's orchestration usage are not independently measured by these receipts.

For a fatal worker exit, the existing driver preserves partial stage artifacts and rejects normal successful scoring. Keep that fail-closed behavior. A source-free failed-batch accounting artifact should retain the four planned outcomes, mark unexecuted/unfinished rows explicitly, preserve all recorded attempts, and keep the primary comparison unavailable; it must not manufacture responses, treat unrecorded usage as zero, or make a partial run appear to be a completed holdout.

Focused offline acceptance tests before registration:

1. Two arbitrary fictional IDs, one case with one version and another with multiple versions. Verify the four ordered result rows and all actual evidence bytes; no old case IDs or exact-two-version assumption may remain. Nullable publication stays valid when exact-version availability is independently represented.
2. Reject duplicate IDs, extra label/adjudication fields, missing source anchors, future/unknown availability, substituted corpus/review/gold hashes, altered code inventory, future-dated registration and an unadmitted case. Check gold is inaccessible from the worker and unavailable to the driver, including when stored outside the repository root.
3. Audit the unchanged direct request, every native schema and exactly placed Candidate 2 suffix. Reject a missing/altered/repeated suffix and any suffix on direct, selection or research. Prove full-source exposure at one and several versions, and reject capacity overflow before invoking a transport.
4. Use fictional scoring labels only to test one affirmative true case and one later-false case. A contradiction on the true case counts as a false accusation; a supported verdict on the false case counts as false acceptance; unresolved never counts as detection. Check cutoff judgments separately from future labels and retain the correct denominator when either pipeline fails.
5. Retain a failed research call followed by a successful formal verdict: primary invalid, formal diagnostic preserved. Retain a timeout with unknown usage, a skipped optional research branch and valid first-verification completion. Exercise call/deadline guards and fatal-worker partial accounting without a network or model call.

A local schema-only reproduction used a completely fictional notice about an imaginary lamp, a `fixture.invalid` URL and synthetic timestamps. The inherited historical validator accepted its single-version structure; the frozen Round 2 wrapper rejected it with “Each case must retain exactly two source versions.” No real control values or source bytes were used in that reproduction. It proves a wrapper compatibility issue, not admission of fictional data or validity of the unseen control's evidence.

Readiness conclusion: the adaptation is bounded and does not require changing production prompts or the model. Implement and test it in a new experiment only after agreeing the source-free contract; registration and inference remain blocked until both real cases independently satisfy admission. This document contains no new evidence, case labels or recommendation to launch now.

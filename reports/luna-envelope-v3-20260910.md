# Luna envelope v3 — 2026-09-10

The fresh policy ran all 16 arms with successful local GPT-5.6 Luna calls. The audit layer now passed, but the double-loop harness produced zero completed/evidence-valid rows in both original and blinded variants; direct achieved 1/4 valid cutoff matches in each variant. The harness therefore did not exceed the unassisted model. This is a real negative result, so the policy is not repeated.

Usage: direct 163,671 tokens; double-loop 165,675 tokens.

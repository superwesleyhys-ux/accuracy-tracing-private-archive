# C8：先检查证据能否相容，再判断真假

**尚未证明超过原始 Astra。本轮的新判定策略已实现，正式模型测试尚未启动。** 准确率仍是优先目标；token只记录，不作为选择策略的标准。

上轮C7的具体错误是把相似图像升级成实质冲突，未充分说明相关观察为什么不能同时成立。本轮C8先编写只描述原文、图像和限定条件的观察记录，再让最终判断重新查看完整原始材料、检查可相容的解释。两个新阶段都不接收前阶段的结论或研究建议。冲突需要同一对象、条件、时间与属性下有依据的对立命题；判假需要明确反证。文字与图片两条harness路径均应用此变化。

Astra单独运行沿用原基线的中性提示词；两组使用相同模型和运行强度。两组均通过本地Codex登录通道运行gpt-6-astra/medium；这里的本地指CLI通道，模型推理仍在远端。两组使用同一份完整文字和原图。过滤输入不能抹去模型训练中可能记住的后续事件。

本轮在新案例评分前固定统一标准：第一手来源能提供可被推翻的初步支持，原始来源身份不自动等于真实；除非具体主张要求独立鉴定，不应普遍要求额外原始记录。旧案例的标签、输出和复核分歧均保留。

| 当前环节 | 结果 |
|---|---|
| C8最初实现和相关离线检查 | 102项通过；4行、20次模拟调用的隔离运行及请求核对通过 |
| 原始JPEG兼容补充 | 9项针对性检查通过；修复了读取EXIF影响PNG验证顺序的问题 |
| JPEG补充后的完整隔离运行 | 4行、20次模拟调用通过；原件字节、尺寸及请求记录保留 |
| 已结束的第一轮资料检索 | 16次请求，2个新事件族，3份文字、1张完整原图 |
| 初始截止日证据复核（保留） | 1项证据冲突、1项证据不足 |
| 新增一手材料的独立补充复核 | 1项获得支持、1项证据不足；真实对照的日期冲突已解决 |
| 当前独立准入 | 1个真实对照合格；另一案例的历史版本对应待补齐，0组可运行配对 |
| 正常浏览器补查 | 1次导航遇到安全验证页；未取得历史原版，配对准入仍未通过 |
| C8真实模型比较 | 0次调用；没有新的准确率或胜出结果 |

原图为734×1044的完整JPEG复合图，尺寸低于目录宣称的更高原始尺寸。模型将直接接收这份JPEG原件；不放大后冒称原始细节。格式支持是在首次模型测试前补充，判定提示词、输出结构、模型配置均与已固定的C8相同。PNG-only版本和修正前的检查记录另行保留；没有重复跑同一策略的模型实验。

当前只剩一个资料缺口：待测案例的完整目标图及配套正文，与2023年底之前实质版本之间的具体对应证据仍不足。新增原始事件记录已解决真实对照的日期冲突，并通过历史版本准入；这是新增材料后的独立补充判断，初始材料和标签保持原样。后续具体证伪主张与2025—2026年份条件已有支持，但这些后续资料不能进入模型的历史证据包。后来的服务器时间不证明内容一定改变，也不能单独证明所取版本就是历史版本。

后续补查完成8次请求，耗时167.78秒；两轮合计24次请求，均已封存。新增一份完整的原始事件记录，但未取得原版PDF，历史版本对应尚未由检索解决。访问遇到429、403、404和返回错误HTML等情况，失败均保留。每次请求均先保存新的问题分解，再发出请求；没有自动重试或自动追随重定向。新增记录以单独补充材料交给独立复核，已获得1个合格真实对照。另一案例的完整图文历史版本仍待补齐，因此目前不能组成正式配对。另一次正常浏览器补查在首个导航时遇到出版方安全验证页，未取得原版图文，随即停止并关闭本次新开的标签页。该次使用1个明确操作，余下4个未用；没有进行验证交互、替换路线或重试。两轮请求与本次浏览器操作合计25个明确外部操作；浏览器自动子请求和自动重定向未逐个计入此数。前24次请求保持封存。

离线检查只验证接线、边界和失败处理，不验证新闻真假。最终比较分别报告截止日证据判断、独立语义复核、后来被证明为假的检出、误接收、误指控、弃答和执行失败。疑似、冲突、弃答或缺少证据都不算提前检出后来被证伪的主张。后续记录支持的具体错误也不自动代表故意造假或整个研究为假。每个正式策略仅运行一次；下一轮模型比较仍需依据具体错误作出实质策略修改。

独立结束复核已核对本次15项记录及此前4份清单引用的232个文件，均未改变；此项是离线记录与文件核对，不是再次访问网站。下一步是另外寻找具有完整历史原件的合格案例，保留已通过的真实对照及当前未通过案例的记录。C8仍未运行；不得把准备完成或补查结束算作模型胜出。

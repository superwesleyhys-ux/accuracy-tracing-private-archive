# Candidate 2: independent two-case historical pilot

This is one prospective paired pilot of direct Astra and the unchanged Candidate 2 harness. It contains one independently corroborated true real-world claim and one narrowly specified real-world claim later corrected as false. The latter establishes a factual error, not fabrication or intent. Admission and all input, label, review, code and scoring commitments must be complete before any live inference.

The input cutoff is 2023-12-31T23:59:59Z. The false case must independently satisfy the exact target/version gate and both separate 2025–2026 dates: formal confirmation and first public establishment of the same falsehood. The independent review binds entity, period, concept, units, qualifications and correction basis. The true control requires affirmative evidence. Historical availability requires evidence for the specific source version; a historical archive hash is not mandatory, and publication metadata alone is insufficient. Preserve actual date precision and document conservative bounds. Later findings, private reviews and labels never enter inference.

Both cases use opaque IDs, and the exact corpus/plan order is fixed before inference. A new derived packet records any ID remapping or metadata precision correction and independently verifies preservation of source text, target meaning and qualifications. Previously frozen control, proposal and discovery files remain unchanged. The source-free admission reports the independently reviewed family count and the documented exposure category; neither an admission flag nor the procedural phase name proves global unseen status.

The model is hosted `gpt-6-astra`, medium reasoning, through the existing local Codex-login HTTP route. Both arms use the same canonical evidence pool and cutoff. Installed local skill catalogs are disabled for both arms. Direct uses the unchanged single-request baseline; the harness executes its research and adaptive formal verification stages. Candidate 2 is exactly the 3,904-character literal with SHA-256 `4c6936fe1e18426a00125a800fa98808bc583ccf7ef9b50d8af54773b866ef79`, appended once to harness decompose/verify instructions only. No live source search or API fallback is allowed. Local orchestration does not make model weights local or erase pretrained knowledge after the cutoff.

Use one complete repetition, with four ordered rows: first case direct/harness, second case harness/direct. Each direct case permits at most one call; each harness case at most 24 total calls, including at most 10 formal calls. Timeout is 90 seconds per call, with 120/2,190-second case deadlines and at most 50 logical calls for the full pilot. Preserve every attempt, failure, natural early completion and skipped optional phase. No selective retry, replacement, extra repetition chosen from its outcome, or expansion of a running budget. A source-only observation or offline stub call is not a benchmark-model invocation.

Each research request receives every supplied eligible version in full; direct receives the same full canonical pool. Versions may number one or several, without manufactured duplicates. The actual count must fit the configured document cap and the full-text 240,000-character capacity. Overflow fails before transport. Research advice remains untrusted, at most 6,000 serialized characters, and cannot create evidence or validate a quotation. Formal selection remains adaptive; report what was actually inspected rather than claiming all phases or two loops occurred.

Primary comparison: cutoff-evidence-correct outcomes divided by the two planned cases in each arm. A primary-valid answer must complete the required pipeline and pass schema, exact canonical evidence and request/response binding audits. Also report exact future-outcome matches, later-false detection, false acceptance, true acceptance, false accusations on the true control, abstention, conflict, and failed/invalid outcomes. Keep a future-false guess separate from a contradiction supported by cutoff evidence. Abstention is not detection. Formal correctness after a failed research stage remains a separate diagnostic and earns no primary credit.

Audit every retained request before parsing scorer-only labels. Bind the exact parsed gold bytes to their registered hash. The scoring component uses the frozen mixed-truth contract, with exactly one real-world true label and one real-world false label; source-specific adjudication records remain private. A fatal worker exit retains all four planned rows, verified partial attempts and explicit unexecuted states, while the primary comparison is unavailable. Unknown dispatch or usage remains unknown. Do not silently drop failed rows or manufacture a successful batch from partial output.

Record input/output/cached token receipts by arm and stage. Unknown usage remains null with known lower bounds. Cached input is already part of input tokens. These receipts do not independently measure internal CLI retries or this task's orchestration, curation and audit usage. Accuracy is the objective; token use does not select the winning prompt, and the arms do not have equal compute budgets.

Before registration, finish offline tests for one/many versions, temporal/label isolation, changed artifact hashes, full exposure and candidate placement, private-file denial including outside-repository files, ordinary research failure, interrupted calls and fatal accounting. Freeze the actual driver, contracts, scorer, dependencies, plan, protocol, corpus, gold, independent reviews and admission hashes. Validation and registration are separate; a synthetic registration cannot authorize live inference.

After execution, retain automatic request/evidence audits and obtain an independent semantic review of the final evidence use before making an accuracy claim. Publish a source-free report with exact counts, failures and limitations. A two-case result is a pilot, even if the harness performs better. It does not complete the user's superiority objective: any apparent gain must preserve true-control accuracy and evidence grounding, and be confirmed on additional independent families that were not used to tune the candidate. Preserve ties and regressions as well as improvements.

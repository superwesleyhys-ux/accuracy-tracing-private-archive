# 后续测试改用 Luna

**原始模型与 harness 两组统一使用 GPT-5.6 Luna、low、本地 Codex 登录通道。停用 Astra，没有 API 或 Astra 回退。持续任务提示词已同步。**

GPT-5.4 mini 被当前登录账号明确拒绝；失败已保留，没有重试。随后 Luna 的一次真实文字＋图片检查成功：算式和图形数量都答对，耗时8.41秒，6,529输入token、20输出token，共6,549tokens。此数仅指该次Luna测试，不包括协调工作；mini失败调用未返回用量。

新运行配置通过离线隔离检查：4条记录、20次模拟调用，包含文字和JPEG路径；实际请求核对已保存。修复了复制后汇总器目录重复嵌套和复核器引用旧哈希的问题。此前3项简单检查不能单独证明完整运行。

**尚未运行Luna的历史新闻准确率比较，没有新胜出结果。** 小型图文检查只证明通道和图片输入可用。旧Astra成绩保持原样，不能当作Luna成绩。C8策略的正式比较仍未运行。

成本要求到达时，资料检索暂停于7次已完成请求，0待处理，0新案例准入。已通过的真实对照及未通过的历史版本案例保留。后续仍须先固定输入、评分和实质策略，每个策略仅登记比较一次。

[官方Luna文档](https://developers.openai.com/api/docs/models/gpt-5.6-luna)确认其面向低成本工作负载，支持文字、图片输入。过滤测试输入不能抹去模型已有历史知识。

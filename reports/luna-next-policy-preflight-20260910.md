# Next policy preflight — stopped before model calls

The proposed next decision-policy change is a single-pass evidence-ledger judgment intended to remove the repeated intermediate harness failures seen in the previous Luna run. Preflight found that the inherited supervisor resolves the double-loop executor from a fixed external `experiments/double-loop-pilot/run_test.py` path. A copied snapshot therefore cannot safely substitute the new policy without changing the worker import path and frozen code commitments together.

No model call, registration, marker reservation, or source request was made for this draft. The earlier two Luna comparisons remain authoritative: the first was scored after corrected registration but lost harness execution completeness; the reliability-gated policy improved some blinded matches but still had failures. This draft is not a result and will not be rerun unchanged.

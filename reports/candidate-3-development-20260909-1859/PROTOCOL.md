# Candidate 3: prospective exposed-development diagnostic

One real paired diagnostic tests a single generic hypothesis: the final verdict must agree with the unchanged target and the explanation's evidence assessment. Candidate 3 must not transfer support for a narrower attribution proposition to an unestablished real-world proposition. The hypothesis came from the independently audited mixed-pilot-0735 failures. These same two cases are therefore **exposed development**, not a new holdout, even though source identities and text remain outside the prompt author's context.

Reuse the exact independently admitted v2 corpus and private gold, preserving their bytes, source versions, target wording, cutoff judgments, later labels and fixed case order. The new source-free admission changes the exposure declaration to previously_exposed; it retains the existing source/input/gold review commitments. No later material enters model evidence. The false case is a narrowly corrected fact, not an accusation of intentional fabrication. One affirmative true control remains essential to assess harmful over-abstention.

The sole treatment change is the reviewed 1,967-character suffix, SHA-256 `ec58f678f267dc81718f360b75cc407df70498be9c1735d148a26d6467e4271c`, after unchanged Candidate 2 with one newline. The 5,872-character combined literal has SHA-256 `289929acb91aef52beec1e4cdf386ecc59a89a1c61469dc93a89b5d971ea2338`. It applies once on harness decompose/verify only. Direct, research and selection instructions, schemas, evidence, historical eligibility and scoring remain unchanged. The scorer uses an independently retained literal, not a runtime mutation of an old helper.

Before this real diagnostic, independently review the eight fictional semantic contrasts in FICTIONAL-SAFEGUARDS-1859-v2.json. They define explicit attribution versus underlying occurrence, estimate versus realization, sufficient first-hand evidence without a second source, source dependence, grounded contradiction and adequate derivation. Run offline contract, placement, full-worker and failure-accounting tests. **Those fictional rubric reviews and fixed-response stubs are not completed semantic model tests.** This real two-case diagnostic directly tests the observed failure and preservation of its real occurrence control; it does not measure all eight fictional contrasts. Never report unmeasured safeguards as passed model behavior.

Use hosted gpt-6-astra, medium reasoning, through the same local Codex-login HTTP path. Both arms receive the full same supplied historical text; local catalogs are disabled. Local describes the route, not offline weights. No source search, API fallback, new source interpretation or hindsight labels in the worker. The 2023-12-31 cutoff cannot erase pretrained model memory.

Register exactly four ordered rows and one complete repetition: first case direct/harness, second harness/direct. Use a fresh direct arm. One direct call and at most 24 total harness calls per case, including at most 10 formal calls; 90 seconds per call and 120/2,190-second case deadlines, at most 50 logical batch calls. No selective retry, favorable output replacement or budget expansion. Keep failures and regressions. The source packet execution fields remain byte-identical; effective formal inner limits derive from the document cap in frozen code. State actual executed phases and rounds; do not claim a second loop when none occurred.

Primary metric is cutoff-evidence correctness with two planned cases per arm and full-pipeline/request/evidence validity. Report later-false detection, false acceptance, true acceptance, false accusation, abstention, conflict and failure separately. The targeted error case's cutoff label remains unchanged. Justified unresolved can improve cutoff agreement but is not detection of its later falsehood. A future-correct guess without cutoff support is not a grounded accuracy win. A drop in true-control acceptance or false accusation counts as a regression.

Audit every actual input/response/receipt against canonical evidence, exact schema and prompt placement before the scorer reads the single hash-verified private gold buffer. Keep exact quotations distinct from semantic entailment. An independent reviewer must inspect the final verdict/rationale scope, necessary versus optional gaps and whether a stronger unsupported explanation merely replaces the original caveat. Ordinary or fatal failures retain the four-row denominator; fatal-worker primary metrics are unavailable and unknown usage stays unknown. Setup/process-creation failures remain explicit incomplete attempts, not manufactured successful batches.

Record all target-model input/output/cached usage by stage and arm. Token cost does not select the candidate; no token optimization is active. Totals exclude orchestration, curation, audit and unmeasured internal CLI retry use. Freeze runner, contracts, local audit helper, scorer, unchanged scoring dependencies, plan, protocol, source-free admission, corpus/gold/reviews and inference inventory before any live call. Preserve every earlier frozen file.

After the one registered run, publish exact results with independent semantic review and compare with the fresh direct arm. Even a favorable outcome establishes only development progress. A superiority claim requires subsequent confirmation on additional independent cases unused in selecting this candidate, with factual detection and false accusations still distinguished. If it ties or regresses, preserve that finding and derive any further hypothesis from the actual audited failure rather than sampling this trial again.

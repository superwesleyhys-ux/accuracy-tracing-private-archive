# Candidate 4: prospective synthetic evidence-role mechanism test

This is a synthetic mechanism experiment, not a historical-news benchmark or a claim of superiority. The active user goal remains independently confirmed accuracy improvement over direct Astra on the user's pre-2024 / later-2025–2026 news task. Candidate 3 failed to improve the exposed real pair. Its independently audited failure motivates one new hypothesis: requiring a cited basis for a stronger claimed evidential role may prevent upgrading an assertion into an established observation or derivation. No real source identities, private real gold or actual model prose enter the prompt-author context.

The unchanged Candidate 3 literal is 5,872 characters, SHA-256 `289929acb91aef52beec1e4cdf386ecc59a89a1c61469dc93a89b5d971ea2338`. Candidate 4 appends one newline and the frozen 2,010-character suffix from its source-blind proposal, SHA-256 `c80c6ea61f0a811093324f3b8a11588956d52e327e6e7ca5f0aa7a1918f69c78`. Both apply only to their harness's existing decompose/verify stages, exactly once. Direct and research-stage prompts, evidence, model, native output schemas and verdict labels stay the same. There is no new role field or automatic semantic verdict correction in this prompt-only intervention.

## Fixed fixtures and conditions

Freeze 14 invented cases s01–s14 as seven adjacent matched pairs. Corpus metadata labels them synthetic; dates and source/version metadata establish only a fictional simulation context. Their gold contains evidence-relative expected labels, no future finding dates or invented later-false labels. Native historical packet shape is reused for controlled text exposure; this does not constitute historical source admission.

The pairs cover unverified allegation attribution versus occurrence; adequate single first-hand evidence with optional unavailable corroboration versus a stronger independent-authentication target; constitutive issuance versus an embedded external claim; dependent repetitions versus an added adequate observation; an assertion of calculation versus supplied executable premises; valid derivation versus one changed premise; and an in-scope counterexample versus superficially similar out-of-scope evidence. Exact fictional content, labels and target-specific semantic criteria must be independently reviewed and frozen before inference. Avoid ambiguity before the freeze. Do not rewrite a label or stipulation to accommodate an observed answer.

Three conditions receive each identical case: direct, candidate3, candidate4. Direct is a fresh Astra baseline; Candidate 3 is a concurrently rerun comparator needed to test this specific prompt change. In fixed case-major order, rotate [direct, candidate3, candidate4] left by case index modulo three. Use one complete repetition, all 42 rows, with no outcome-selected reruns. Fourteen cases do not balance every position perfectly; the 5/5/4 rotation is predetermined rather than selected from outcomes. All three are the same hosted gpt-6-astra with medium reasoning through the local Codex-login HTTP route. Local describes transport, not local weights.

Installed skill catalogs are disabled in all arms. The isolated worker receives full eligible fictional material, native inference code and execution policy, not gold, role labels, rubric, reviews, registration or any real-news source packet. Source-network access is disabled. No API fallback, extra retrieval or prior output substitution. Hash canonical files and the exact worker copies. Audit every actual input, instruction placement, evidence version/offset, schema, raw response and receipt before the scoring process reads and parses the single hash-verified gold buffer.

## Bounds and failure treatment

Keep existing per-case controls: direct one call, each harness at most 24 total calls including at most 10 formal attempts, at most 90 seconds per call, and per-case deadlines of 120 seconds direct / 2,190 seconds harness. The maximum is 14 × (1 + 24 + 24) = 686 logical invocations. Count interrupted/pre-dispatch recorded attempts according to the frozen accounting contract; do not silently reset a cap or extend deadlines. Record the effective internal limits from execution, rather than claiming all serialized intake settings govern the formal loop. A completed early answer does not prove that both loops executed.

Retain failures and all planned rows. Ordinary failed or invalid pipelines receive zero primary label-agreement credit. If the started worker fatally terminates, preserve its journal, actual partial calls, unknown usage and unexecuted rows, with primary comparison unavailable. A setup/process-creation failure remains an incomplete launch and does not manufacture a batch. Output directories and prior scoring artifacts are never overwritten; no selective retry of inconvenient answers. The post-run source-free summary cannot itself prove semantic validity.

## Primary mechanism assessment and safeguards

Automatic results are **label agreement**, not proof of evidence-to-target entailment. Report each arm's exact matched-label count / 14, every error, unresolved/conflicting/failure count, true-control acceptance and false accusation, attribution and derivation control performance, and pair-level results. Generic role strata in the scoring gold do not enter inference. No synthetic future-falsity metric is constructed.

Independently assess the actual decisive evidence-to-target inference against the frozen fictional rubric. Identify a supported basis for every stronger claimed role, not merely a matching quotation or role vocabulary. Count unsupported evidence-role promotion, attribution substitution, unjustified claims of independence/authentication, invalid or unexecuted derivations, target-scope errors and unsupported certainty. Record ordinary adequate first-hand, constitutive, attribution and derivation support, and grounded contradiction. Missing evidence alone must not become a false accusation.

For this semantic review, prepare output records that omit arm names and instructions and use opaque response IDs with a separately retained mapping. Keep reviewers unaware of the mapping until their judgments are frozen. Prose can reveal a condition indirectly; do not claim guaranteed blinding. Reviewer access to fixed fictional packets and the independently frozen rubric is necessary. No new model-judge call or external search is authorized by this protocol; independent agent review is recorded separately from target-model usage.

The mechanism advances only if Candidate 4 reduces independently confirmed role/target errors against Candidate 3 and preserves adequate true/constitutive/attribution/derivation support without increasing false accusations. Report the direct comparison alongside this causal diagnostic. A label improvement with an unsupported rationale does not establish the mechanism. A prettier explanation with the same unsupported inference is failure. If Candidate 3 already passes all contrasts, report a ceiling: it establishes no Candidate 4 benefit and does not justify rerunning the unchanged exposed real pair hoping for a favorable sample. If all arms pass, do not call the synthetic cases proof of superiority.

These small, designed contrasts provide development evidence only. Even a clear mechanism result does not satisfy the active goal. Any subsequent real development test requires its own fixed plan and fresh baseline; any broader advantage requires prospective evaluation and confirmation on additional independent event families not used to select the prompt. The real historical cutoff/future-label gates stay unchanged. Abstention may repair unsupported certainty but is not detection of later falsity. Prompt changes cannot establish unavailable later truth when the eligible evidence does not distinguish it.

## Registration, reporting and next action

Before the first live call, freeze this protocol, plan, independently admitted synthetic corpus/gold, input/gold reviews, driver/contracts, independent instruction audit literals, scorer, pure scoring component and inference inventory. Verify old frozen experiments unchanged. Run offline safeguards for each arm's exact placement, worker isolation, all-row/fatal/unknown accounting, full evidence, schema/quote binding, malformed plans and gold-read order. Fixed-response stubs do not demonstrate semantic behavior.

Record all real target-model token usage by arm and stage, including failures and unknowns; cached tokens are already part of input totals. Totals exclude source curation, orchestration, review and unmeasured CLI-internal retries. Accuracy, not token savings, selects the next intervention. Publish all results and independent review, including ties and regressions.

If this prompt-only intervention fails, do not automatically stack another warning onto it. Use the actual audited failure to determine whether new evidence, a different mechanism, or a separately registered structured evidence-role representation is needed. Schema/host-check changes require a distinct controlled design; exact-span validation alone still cannot authenticate observations. Preserve the user goal and the existing continuation rather than declaring completion from a synthetic or exposed development result.

## Preflight v2: preserve unissued guard failures

Independent review reproduced a pre-dispatch deadline/budget rejection that adds an enclosing native model-IO failure without sending an actual request. The first prepared runner would reject the batch's audit on unequal counts. Its frozen files and initial code inventory are preserved as preflight artifacts, with zero live invocations. A new semantic-runner-1929-v2 retains the same prompts, fixtures, labels, model route and limits while binding these specific guard failures separately from dispatched requests. They must remain ordinary failed rows in the full denominator, with no invented successful call or usage. Unexplained missing/extra records still reject. Add and independently verify a focused regression before registration.

The registered experiment uses the v2 executable inventory and this protocol. Arm masking uses prepare_blind_review_v2.py, committed and tested before inference. Earlier protocol, inventory and masking validation are preserved; none represents a completed real-model trial. Independent real-news source intake may proceed separately toward the broader user goal, under its own pre-registered source operation cap and per-request prompt decomposition. Such new real evidence never enters this synthetic worker or changes its frozen fixtures.

The reviewer-facing final basis is projected to the common version_id/quote fields. Raw direct and harness responses differ in whether offsets are present, so preserving that signature would disclose arm type deterministically. Original offsets remain unchanged in the audited raw result, bound by the private mapping's result hash. The common projection preserves quotation text and source identity for semantic review without claiming prose can be perfectly blinded.

The exact arm-masking script committed for this trial is SHA-256 `e167067e107ed151a50d87c38a9165c09fd053a4db7c47dfbb640e218783753a`; its common-basis projection is assessed by the independent pre-registration review and the recorded actual-stub validation. Any change requires a new declared projection revision before review, with the original retained.

# Prompt candidate 2: exact comparison binding

This protocol must be committed with the exact guidance, executable inventory and registration before inference. All four results and unsuccessful attempts remain recorded.

## Hypothesis and scope

Candidate 1 completed all four registered outcomes with no primary-score change. Its h06 final explanation overgeneralized the meaning of different percentages elsewhere in the source. The new hypothesis is that requiring an explanation to bind each compared passage to its own quantity and scope will prevent that unsupported generalization, without changing supported attribution or turning missing authenticity records into a false verdict.

Candidate 2 preserves the complete Candidate 1 guidance and adds one generic instruction block from `CANDIDATE-2-PROPOSAL.md`. The exact combined literal is independently copied into this round's driver and scorer; its SHA-256 is `4c6936fe1e18426a00125a800fa98808bc583ccf7ef9b50d8af54773b866ef79`. The proposal SHA-256 is `84d68df7d942433a6d8579723a859772a2ab65becf8061fae4cc7eb1f5e81847`. It applies only to the harness decomposition and verification stages, after the existing instructions. Direct Astra, all research instructions, schemas, historical target, evidence, orchestration, temporal instruction, scoring and operational limits are unchanged. There are no source names, event-specific quantities, later outcomes or answer labels in the new instruction. Production prompts remain unchanged.

The exposed development cases are h05/h06 from one wastewater event family. This is a targeted regression diagnostic. A repair to an explanation is not a fabrication-detection improvement. Both preceding direct arms already achieved the maximum 2/2 cutoff agreement, and the unchanged authenticity cutoff gold remains unresolved. A later-false guess cannot establish a grounded win. Do not treat these exposed cases as a holdout or as independent replications of different events.

## Fixed comparison

PLAN.json fixes h05 then h06 and fresh direct/news_tracing arms, once each. Order remains h05 direct then harness, h06 harness then direct. Retain all four outcomes, every invocation, failures and unknown usage. No selective retries, replacement batches or API fallback. Both arms use local Codex-login HTTP to hosted gpt-6-astra with medium reasoning. Local describes orchestration, not offline model weights. Source networking remains disabled.

Limits remain 90 seconds per invocation, one direct call, at most 24 shared harness calls including at most 10 formal calls, and 50 batch calls. Case deadlines remain 120 and 2190 seconds. Internal CLI retries are not separately controlled or counted. Token usage is recorded only and does not determine candidate selection.

## Evidence and isolation

Preserve the original eight-case corpus, gold, preflight and independent-review commitments. Copy only the selected h05/h06 non-biological packets into the isolated worker. Every research invocation receives all supplied eligible text versions, with exact offsets, content length and availability metadata. Same-URL versions are not independent corroboration. The 240000-character research capacity fails before inference on overflow; no silent truncation. The direct arm receives the entire canonical pool. Formal inference retains the existing version-selection behavior and bounded, explicitly untrusted research advice.

The supplied source text contains no newly recovered table, figure or underlying measurement record. No new source is introduced and the live HTML table-extraction fix is not exercised. The worker cannot read the repository, gold, private intake, output roots, plan, protocol, proposal, scorer or registration. Only selected inputs, frozen inference code and execution policy enter it. Every request retains the temporal prefix and exact historical target. Time-limited evidence access cannot erase pretrained model memory.

Freeze this protocol, plan, driver, scorer and executable inventory before inference. Register their hashes together with the unchanged historical commitments. Preserve Candidate 1 and every preceding artifact. A change after registration requires a new round; do not edit a running or completed round's frozen artifacts.

## Primary scoring and targeted review

After the batch completes, audit every actual instruction, packet, schema, response, receipt, source/version binding, full-text research exposure, advice boundary and code commitment. Require the exact combined Candidate 2 suffix once on each harness decompose/verify call and nowhere else. The direct prompt remains byte-exact unchanged plus the original temporal prefix. Only after these audits may the scorer read future gold.

Primary measures remain original cutoff/future verdicts and evidence validity. Full-pipeline validity includes research failures. Invalid or failed pipelines receive no detection or abstention credit. Preserve all supported, contradicted, conflicting, unresolved and invalid outcomes. Unknown usage remains null with known lower bounds; cached input is already part of input tokens.

An independent reviewer will separately inspect all formal outputs and both direct responses. For each explanation of an apparent numerical or textual discrepancy, check the exact compared passages, quantity meanings, units, denominator/population, time and conditions. Distinguish a source-supported resolution from an untested possibility. Reject broad classifications that the cited passage does not establish, and check that unavailable figures or operands are not claimed as inspected. Check final target preservation, missing-record abstention and attribution correctness. Recompute arithmetic if any is actually claimed. Failure to make the old erroneous assertion is not evidence that a new comparison was performed; report avoidance and executed valid checks separately.

This is a qualitative, preregistered secondary diagnostic, not a new primary score. Compare with the retained Candidate 1 defect without claiming causal certainty from one stochastic trial. Do not change gold or reward longer prose, extra anomalies, or ungrounded rejection. Tests with stubs validate transport/isolation/schema boundaries only, not model reasoning. Independent unseen families with affirmative true controls and later-confirmed false claims remain necessary before claiming superiority.

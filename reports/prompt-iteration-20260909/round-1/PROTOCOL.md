# Prompt candidate 1: supplied-evidence checks

This is a new experiment profile on h05/h06, two previously exposed wastewater development cases from one family. It is not a holdout, balanced authenticity benchmark, or superiority test. No production prompts, original corpus, labels, or previous results are modified. The direct baseline already achieved the maximum 2/2 grounded cutoff agreement on this fixed set. Authenticity remains unresolved under the registered cutoff gold; a later-false guess cannot be called a grounded detection win.

## Candidate and unchanged comparison

The reviewed Candidate 1 proposal has SHA-256 `d25a4d8e7a0faf8415e868e98bf3f138818dd63c3223e1c0c2b8fa2ce944b2c1`. Its 2,909-character guidance has SHA-256 `2ce1b0de9ff5ca756980f76124c31696caac88157adb26c784ab1ea50d42afe7`. Independent literal copies are frozen in the driver and scorer. The experiment transport appends exactly one newline and that block after the existing harness decomposition and verification instructions. The original temporal instruction remains first. The direct baseline, selection, research instructions, schemas, source packets, and existing untrusted-advice boundary are unchanged. The inner model_io retains original instructions; actual request sidecars retain the augmented instructions, and the scorer checks their exact relationship.

The guidance requests at most three relevant scope, falsification/consistency, or interpretation checks. It requires supplied operands for a calculation, preserves benign scope or rounding explanations when supported, and distinguishes attributed statements from actual measurement authenticity. Check summaries occupy existing fields. They do not become evidence, introduce a new call, or force a verdict. No candidate is installed in production by this experiment.

PLAN.json fixes h05 then h06 and fresh direct/news_tracing arms. Order alternates: h05 direct then harness; h06 harness then direct. All four outcomes remain the denominator, including malformed results, failed research, timeouts and invalid citations. Each arm runs once. There are no orchestration retries, selective reruns, replacement of failed batches, or API-route fallback. Incomplete supervisor batches retain their artifacts and cannot silently be replaced with a successful score.

Both arms use the local Codex-login HTTP route to hosted gpt-6-astra, medium reasoning, at most 90 seconds per logical invocation. Local does not mean local model weights. Direct has one call; the claim-mode harness has 24 shared calls including at most 10 formal calls. The batch ceiling is 50 calls. Case deadlines are 120 and 2190 seconds. Internal CLI retries are not independently controlled or counted as separate logical invocations. Source networking is disabled.

## Evidence, isolation and freeze

The supervisor verifies the unchanged original eight-case corpus hash, preflight, original helper and independent review commitments. Only the two selected non-biological cases are copied to the isolated worker. Both canonical versions remain unchanged. Research receives every eligible text version in full with exact offsets, content length, published_at, retrieved_at, available_at and availability_basis. Same-URL versions are distinct versions of one source, not independent corroboration. Total research capacity is 240000 characters; overflow fails before a call rather than truncating. Direct and formal verification retain the same complete canonical pool and eligibility rules.

Full extracted text is not underlying raw measurements, separate result tables, original figure files, or inspected images. This round adds no new source or attachment. It does not exercise the separate live-HTML table extraction fix. The previous full-evidence diagnostic already supplied these complete texts and availability fields.

Before inference, freeze this protocol, PLAN.json, driver, scorer and the current executable inventory. Register those commitments with the unchanged corpus, gold, helper and independent-review hashes. The worker is denied the original repository, corpus directory, private/output roots, review, protocol, plan and registration files. Only selected inputs, an execution policy and frozen inference code enter the worker. No scorer, future-gold contents or proposal document is copied there. Every invocation retains the temporal instruction and exact historical_target.

## Scoring and secondary review

After all four outcomes are archived, audit all actual instructions, packet contents, source/version membership, exact full-text exposure, research scope, advice, native schemas, raw responses, receipt bindings, stage caps and frozen hashes. Require the exact candidate suffix once on harness decompose/verify and nowhere else. The direct actual instructions must exactly equal the unchanged baseline plus original temporal prefix. Only after these audits may the scorer parse the original future gold.

Primary results retain the original exact-quote grounding, cutoff and future-label scoring. Whole-pipeline validity includes research errors; formal-only validity remains a separate diagnostic. A failed pipeline cannot receive abstention or detection credit. Report supported, contradicted, conflicting, unresolved and invalid separately. Preserve unknown or unchanged outcomes, unsupported certainty, failure denominators and all receipts. Tokens, cached input, phase cost and latency are descriptive diagnostics, not a substitute for accuracy. Unknown usage remains unknown with measured lower bounds; cached input is already included in input tokens.

An independent manual reviewer will examine all actual formal outputs after scoring for observable scoped checks, exact quotations, supplied numerical operands, compact calculations, valid denominators/units, benign alternatives, and explicitly unexecutable checks. Recompute any claimed arithmetic from the admitted operands. Do not infer execution from a model's claim that it checked something, and do not score more anomalies or longer prose as success. Check-quality findings remain secondary process observations, never automatic fabrication-detection credit. Retain the archive-availability distinction and do not treat missing records as falsehood. This manual review does not revise labels or primary scores.

No deterministic stub can validate the prompt's real reasoning quality. Stubs verify routing, exact placement, evidence exposure, isolation, schemas and failure accounting only. The actual model run remains necessary to test the behavioral hypothesis. No paid model judge is added.

If a model identifies a previously missed decisive passage, independently review the source and annotation before any separately registered benchmark change. Preserve this round's original score. Never revise gold after seeing a preferred answer and call that an accuracy improvement. Temporal input restrictions do not erase training memory. This fixed exposed set cannot demonstrate superiority over an already perfect baseline.

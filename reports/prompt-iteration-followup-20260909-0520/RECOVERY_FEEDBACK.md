# Historical source-recovery feedback, 05:20 continuation

The independent review supports recovery of one pre-cutoff linked seller-page
replay, not the complete original advertisement. No new case is admitted.
Keep the recovered raw response, extracted text and all previous failed attempts.
The preserved operation log contains 12 operations; its full search responses
and some per-operation timestamps are missing, so it is not a complete independent
record of historical tool activity. Do not reconstruct missing receipts or
retroactively mark this limitation resolved.

The next bounded source-only pass should read the distinct institutional-route
lead in `private/heartbeat-0520-source-recovery/RESUME.md`. Prompt-tuning context
must continue to read only safe statuses and aggregate reviews. The target is the
actual historical advertisement and applicable conditions, with exact binding to
the later factual finding; another generic seller-page snapshot is insufficient.

Apply the independent `ADMISSION-INTERPRETATION-0520.md` without changing prior
labels or date gates. Distinguish literal falsity, misleading presentation,
missing substantiation, and other violations. Keep public allegations separate
from public evidence establishing the exact factual proposition.

For each future operation, preserve its ID, start/end timestamp, requested source
or query, tool type, full returned response (or exact error), and content hash
before deriving a discovery summary. Count search queries individually even when
batched. State which tool-internal activity remains unobservable. Never describe
a summary-only log as a complete independently reconstructed retrieval record.

Keep historical page availability separate from identity with the advertisement
at the target campaign time. The recovered snapshot follows the promotion window;
being earlier than the global cutoff does not prove it is the same relevant
version. A link in a currently retrieved article bearing an old publication date
does not establish that the link existed in the old article version.

Retain archive-added markup separately from the original content representation.
If producing a later inference packet, exclude archive navigation and later UI
content with a reproducible extraction/mapping; do not silently delete original
qualifiers. The observed image count includes archive UI, and no original image
binary was recovered. Do not call HTML image elements independently inspected
original assets. If layout determines the conveyed proposition, recover and
independently review the actual visual context before admission.

The archive URL, availability record and body timestamp are internally consistent,
and text extraction is reproducible. No original WARC payload/CDX digest was
recovered. Do not claim original-payload cryptographic verification from replay
consistency alone. Preserve the evidence strength and remaining uncertainty.

No equivalent model rerun is warranted by this source-only result. Once an exact
eligible later-false case has been independently admitted, register a new paired
evaluation with the frozen candidate and the reviewed true control. Until then,
continue a substantive source-admission step and preserve the existing scores.
